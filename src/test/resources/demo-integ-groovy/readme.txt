A demonstration of how a groovy thread could be started to poll from an activemq queue which could be accessed from java.
The message is constructed as a domainMessage and the service is intended to read the metadata of the request and serve accordingly, 
Suited for integration with java for templating use case